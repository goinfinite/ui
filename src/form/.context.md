<context path="src/form" updated="2026-09-18">

Form input components for collecting user data. All components support Alpine.js two-way binding and have accessibility features (labels and disabled states); applicable components also support hints.

## Summary

- README.md — Component list and usage snippets for the package
- checkboxInput.templ — Checkbox with square, rounded, or circular shapes, five sizes, box colors, label position, indeterminate state, and static or Alpine-bound checked and disabled states
- checkboxInput_test.go — Table-driven tests for the checkbox size and shape class resolvers
- inputField.templ — Text input with configurable type (text, email, number, date, etc.), hints, and affixes
- toggleSwitch.templ — Toggle switch for boolean or array Alpine.js state with size, color, and label position settings
- inlineRadioGroup.templ — Horizontal radio button group with multiple options
- radioInput.templ — Single radio button input
- selectInput.templ — Dropdown select with flat or label-value options rendered as native radio rows that share the component InputName as their radio group
- multiSelectInput.templ — Dropdown multi-select with checkbox options
- multiSelectInputState.js — Alpine.js data component for multi-select dropdown toggle behaviour
- textArea.templ — Multi-line text input with five height steps, expand-to-3x toggle, focusable action buttons, and hints
- inputHint.templ — Shared hint renderer: focusable info-icon tooltip with ARIA wiring, or description line below the field

## Constraints

- MUST include InputName for form submission compatibility (FormData)
- MUST support InputId for optional DOM reference where a native input renders; selectInput has none because its radios carry the shared InputName instead
- MUST support two-way binding via TwoWayStatePath for Alpine.js
- MUST include IsRequired flag and visual indicators
- MUST render hints through inputHint.templ so the tooltip stays keyboard and touch accessible

## Guidance

- SHOULD support hint/description text with configurable display (tooltip vs description)
- SHOULD support prefix and suffix affixes (left/right) with values or state binding
- SHOULD include proper error state styling and messaging support

</context>
